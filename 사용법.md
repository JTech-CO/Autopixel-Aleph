# AutoPixel-ℵ 사용법 (v2.1.2)

칸마다 **포인터 이동 > `i`(색상 인식) > 클릭 > 클릭** 을 보내고, 각 단계 사이를 렌더 프레임 단위로
기다립니다. 실제 마우스는 건드리지 않습니다.

모든 조작은 패널에서 합니다. 단축키는 없습니다.

## 1. 준비

1. `chrome://extensions` > **개발자 모드** > **압축해제된 확장 프로그램을 로드** > `autopixel-x` 폴더
2. `https://wplace.live/` 또는 `https://youplace.live/` 접속 후 도안 오버레이를 맵 위에 띄웁니다.
3. 화면 왼쪽 위에 패널이 나타납니다. 헤더를 잡고 옮길 수 있고, 오른쪽 위 화살표로 높이를 3분의 1로
   접을 수 있습니다. 접어도 진행 바·시작/정지·상태줄은 그대로 보이므로 실행 중에 접어 둬도 됩니다.

`오버레이 (i)` 모드는 맵 위에 도안이 그려져 있어야 합니다. 사이트의 `i` 스포이트가 포인터 아래 색을
집어 오기 때문입니다. 오버레이 없이 쓰려면 색상 소스를 `현재색`으로 바꾸고 팔레트에서 색을 직접
고르세요.

한글 IME는 꺼둘 필요가 없습니다. 실제 키보드 대신 `KeyI` 이벤트를 직접 보내므로 입력기 상태와
무관합니다.

## 2. 1칸 크기 보정

줌 레벨마다 한 번씩 하면 됩니다.

1. **보정** 을 누릅니다. 화면이 십자 커서로 바뀝니다.
2. 아무 칸의 **중앙**을 클릭한 뒤, **`칸수`(기본 10)만큼 떨어진 칸**의 중앙을 클릭합니다.
3. `12.40 px` 처럼 측정값이 표시됩니다.

보정 중에 취소하려면 **정지** 를 누르거나 **보정** 을 다시 누르면 됩니다.

`칸수`가 클수록 정확합니다. 한 칸만 재면 그 오차가 전체 칸수에 그대로 곱해지지만, 여러 칸에 걸쳐 재면
오차가 그만큼 나뉩니다. 65칸 내외라면 10~20을 권장합니다.

격자가 도안에 안 맞으면 `1칸` 줄의 두 가지로 조정합니다. `-` / `+` 는 1칸 크기를 0.05px씩 바꾸고,
그 옆 화살표는 격자 전체를 1px씩 옮깁니다.

줌을 바꾸거나 창 크기를 바꾸면 보정값이 무효가 되며, 패널이 알려 줍니다.

## 3. 구획 선택

**구획 선택** 을 누르고 도안 위로 드래그합니다. 놓으면 격자에 맞춰 스냅되고 `8 x 5 = 40 px` 처럼
표시됩니다.

- **격자 표시** 로 격자선을 켜고 끕니다 (칸이 5px 이상, 구획이 4만 칸 이하일 때 그려집니다).
- `구획` 줄의 화살표로 구획을 한 칸씩 옮깁니다. **지우기** 는 구획만 초기화합니다.
- 화면 밖 칸은 찍을 수 없습니다. 상태줄이 `N칸 화면 밖`으로 알려 줍니다.

## 4. 실행 설정

### 속도

| 프리셋 | 프레임 | 페이스 | 60Hz 기준 칸당 ms | px/s |
|---|---|---|---|---|
| **안전** | 10 | 50 % | 336 | 3.0 |
| **빠름** (기본) | 4 | 75 % | 91 | 11.0 |
| **터보** | 4 | 100 % | 68 | 14.7 |

터보가 전력 주행입니다. 빠름은 입력 타이밍이 터보와 같고 속도만 75%로 묶입니다. 안전은 50%로 묶이면서
키·버튼을 더 오래 누르므로 칸이 누락될 때 되돌아갈 등급입니다. 페이스는 가정값이 아니라 칸마다 실제
비용을 재서 정합니다. 사이트 자체가 병목이 되면 프레임 하나쯤은 페이지가 어차피 하던 일에 묻히기
때문입니다.

**직접(Custom)** 은 네 값을 직접 만질 수 있고 페이스가 걸리지 않습니다 (속도를 낮추려면 딜레이 사용).

- **이동** 포인터 이동 후 기다리는 프레임. 페이지가 좌표를 반영할 시간
- **누름** 키·버튼을 누른 상태로 두는 프레임. 최소 1, 그보다 낮으면 입력이 누락됩니다
- **간격** 각 단계 사이 프레임
- **클릭** 칸당 클릭 횟수, 기본 2

칸이 누락되면 **누름** 부터 올리세요.

### 색상 · 순서 · 제한

- **오버레이 (i)** 는 칸마다 `i`를 눌러 도안 색을 인식합니다. **현재색** 은 `i`를 건너뛰고 팔레트에
  선택된 색으로 찍어 약 1/3 빠릅니다. 단색 영역에 적합합니다.
- **지그재그**(기본) · **행** · **열** · **무작위**. 실제 마우스가 움직이지 않으므로 속도 차이는
  없고, 무작위는 규칙적인 진행을 피하고 싶을 때 씁니다.
- **딜레이** 는 칸 사이 추가 대기(ms), **지터** 는 그 값을 흔드는 비율(%)이며 둘 다 기본 0입니다.
  **최대** 는 이번 실행에서 찍을 칸 수 상한으로, 충전량에 맞춰 끊어 갈 때 씁니다.

### 보호 옵션

- **맵 보호** 는 휠·드래그·타이핑이 감지되면 멈춥니다. 맵이 움직이면 보정값이 전부 틀어지므로 켜 두는
  편이 안전합니다.
- **캔버스만** 은 클릭 지점이 그리기 표면이 아니면 그 칸을 건너뜁니다. 사이트 UI를 잘못 누르지
  않습니다. 표면은 보정한 위치에서 학습하고 `<canvas>` 는 모두 인정하므로, 맵 위에 도안 오버레이가
  따로 얹혀 있어도 괜찮습니다. 막힌 칸은 `N칸 페이지에 가려짐` 으로 표시됩니다.

탭이 보이지 않게 되면 자동으로 멈추고, 돌아오면 이어서 진행합니다.

## 5. 실행

**시작** 을 누릅니다. `120 / 400 · 14.6 px/s · 19s` 형태로 진행 상황이 나오고 찍힌 칸이 오버레이에
채워집니다. 같은 버튼이 **일시정지** → **재개** 로 바뀌고, **정지** 로 끝냅니다. 시작 전 예상치는 이
기기에서 실제로 측정한 프레임 간격을 쓰므로 120Hz 모니터에서도 맞습니다.

## 6. 문제 해결

| 증상 | 확인할 것 |
|---|---|
| 아무것도 안 찍힘 | 탭이 앞에 있고 보이는지. `안전`으로 시도. `누름`을 1 이상으로 |
| 칸이 누락되거나 색이 틀림 | `누름`·`간격`을 올리거나 `안전`으로 확인 |
| 격자가 도안과 어긋남 | `1칸` 줄의 `-` / `+` 와 화살표로 조정, 또는 `칸수`를 키워 재보정 |
| `N칸 화면 밖` | 구획 일부가 창 밖입니다. 맵을 옮기거나 구획을 줄이세요 |
| `N칸 페이지에 가려짐` | 그 칸들이 사이트 UI 아래에 있습니다. UI를 치우거나 구획을 옮기세요 |
| `"캔버스만"이 모든 칸을 막았습니다` | 가드가 이 사이트의 그리기 표면을 인식하지 못했습니다. 다시 보정해 학습시키거나 **캔버스만** 을 끄세요 |
| 시작하자마자 멈춤 | **맵 보호** 가 실제 클릭·휠·타이핑을 감지 |
| 예상보다 느림 | 백그라운드 탭은 프레임이 스로틀링됩니다. 탭을 앞에 두세요 |
| 도안 색이 아닌 색 | 오버레이가 실제로 그려져 있는지, 소스가 `오버레이 (i)`인지 확인 |

## 7. 주의

- 실행 중에는 맵을 움직이지 마세요. 줌·이동이 일어나면 좌표가 전부 어긋납니다.
- 페인트 충전량을 소모합니다. 시작 전 `가로 x 세로` 를 확인하고, `최대` 로 끊어 가세요.
- 자동화는 사이트 규정에 위배될 수 있고, 그 결과의 책임은 사용자에게 있습니다.
