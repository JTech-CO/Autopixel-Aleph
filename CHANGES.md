# 2.2.0 (overlay fix 1) — 공식 도안 초기 감지 수정

Wplace 공개 사이트의 모듈 파일명이 바뀌었는데 감지 코드는 이전 이름에 고정되어 있었습니다.
그 결과 도안 데이터를 읽기 전부터 native-unsupported로 중단됐습니다.
버전 재번호 작업에서 src 기능 코드는 버전 문자열 외에 바뀌지 않았으며,
이번에는 새 사이트 빌드와의 연결 문제를 수정했습니다.

- 검증된 신·구 빌드의 모듈 세트를 구분해 연결합니다.
- 새 빌드: B6mxrfTb.js / Db9HDRn2.js / DKrwebdO.js / B6e74eJx.js.
- 기존 빌드: tX2H6UC0.js / C3OwBbQa.js / Dk0Q_kgI.js / D8DZ-h5y.js.
- 동일 출처의 관찰된 모듈 URL만 사용합니다. 임의의 파일명이나 모든 모듈을 불러오지 않습니다.
- Resource Timing 기록이 지워진 경우 공식 module script / modulepreload 요소에서도 확인합니다.
- 모듈 미감지, 로드 실패, 인터페이스 불일치, 도안 데이터 오류를 구분합니다.
- 칠하기 엔진과 속도 상한은 바꾸지 않았습니다.
- 숫자 버전은 요청대로 2.2.0을 유지하고 version_name으로 수정본을 구분합니다.

수정 전 브리지가 새 모듈 이름에서 0칸 처리 후 같은 오류로 중단됨을 재현했습니다.
수정 후 동일 조건의 도안 처리와 Alliance 검사가 통과했습니다.
공개 Wplace 페이지에서도 실제 모듈 인터페이스(64색 팔레트, 지도 캔버스,
환경 설정, 미리보기 구독, Alliance 렌더러)를 읽어 확인했습니다.
실제 페이지 연결 검사는 사용자 도안이 없는 상태에서 정상적으로 native-overlay로
종료되는 것까지 확인했으며, 로그인 계정으로 픽셀을 칠하거나 서버 저장을 검증하지 않았습니다.
모르는 향후 사이트 빌드에는 별도 호환성 수정이 필요할 수 있습니다.

검사 결과: tests/validation-summary.json, tests/native-results.json,
tests/live-module-check.json.
공식 확인 출처: https://wplace.live/ 및 해당 페이지가 참조한 /_app/immutable/chunks/ 모듈.

적용: 새 ZIP으로 확장을 갱신 → Wplace 탭 새로고침 → 공식 도안 다시 열기.
'오버레이 (i)' / '공식 자동' 설정을 사용합니다.

---

# 2.2.0 — 색 확인 중 반복 중단 수정

## 확인한 결함

2.2.2는 도안의 기대 색 버튼이 추출 전에 DOM에 존재하고 표시되어야만 진행했습니다.
공식 Wplace는 색 추출 후 추가 팔레트를 펼칠 수 있으므로 이 순서가 맞지 않습니다.
또한 추출 전에 저장한 버튼 객체를 계속 비교해, 팔레트가 재생성되면 색이 맞아도 실패했습니다.

이전 엔진을 같은 브라우저 재현 환경에 넣어 첫 색 1칸 후 다음 색에서 중단되는 것을 확인했고,
수정 엔진은 같은 4칸을 모두 처리했습니다. 제보 화면만으로 사용자의 정확한 실패 단계까지
확정할 수는 없으며, 이번 수정은 확인된 두 결함과 일시적 응답 지연을 처리합니다.

## 변경

- 기대 색 버튼은 추출 뒤 현재 DOM에서 다시 찾습니다. 팔레트를 미리 수동으로 펼칠 필요가 없습니다.
- 색 추출 ‘완료’와 도안 색 ‘일치’를 별도로 확인합니다. Alliance는 테두리 표시가 아니라 피커 모드 전환으로 완료를 판정합니다.
- 완료된 오색 응답은 같은 칸에서 최대 3회 확인합니다. 계속 불일치하는 칸은 칠하지 않고
  나머지를 진행하며, 종료 시 ‘색 재확인 실패 N칸 미도색’을 표시합니다.
- 대기 시간을 650ms에서 최대 2초로 늘렸습니다. 정상 응답에는 이 시간을 추가하지 않습니다.
- 실제 응답이 없는 요청은 재전송하지 않습니다. 중단 후 원래 응답이 완료되면 다음 실행에서
  그 완료를 확인하고 재개할 수 있습니다. 영구적인 재시작 차단 플래그를 제거했습니다.
- 피커 전환 실패, 이전 추출 응답 대기, 완료된 색 불일치를 구분해 안내합니다.
- 진단 결과에 마지막 확인 단계, 기대 색, 관찰된 색과 시도 횟수를 포함합니다.
- 종료 시 공식 비교 세션 정리가 끝나기 전에 새 실행이 시작되는 경합을 방지했습니다.
- 공식 자동 비교, 투명/동일 색상 건너뛰기, Alliance, 10/20/30px/s 상한은 유지합니다.

## 검증

tests/native-regression.cjs에 아래 조건을 추가했습니다.

- 2.2.2 중단 재현과 동일 조건의 2.2.0 성공
- 추출 후 팔레트 버튼 생성 및 매번 DOM 교체
- 첫 응답만 이전 색인 상황의 자동 복구
- 850ms 추출 지연 처리
- 반복 오색의 재시도 상한과 미도색 처리
- 정지 후 늦은 응답을 기다리고 재시작
- 게임 페인트 창이 닫혀 있을 때 클릭 전 진단

전체 결과는 tests/validation-summary.json과 tests/native-results.json에 기록합니다.
검증은 로컬 Chrome 재현 환경이며, 실제 로그인 계정의 오류율이나 서버 저장 성공률을
검증한 것은 아닙니다. 기존 2.2.0~2.2.2 레거시 폴더는 삭제했습니다.

## 적용

확장을 2.2.0으로 갱신하고 Wplace 탭도 한 번 새로고침합니다.
‘오버레이 (i)’와 ‘공식 자동’을 그대로 사용합니다.
미도색으로 남은 칸은 원인이 해소된 뒤 다시 실행하면 됩니다. 이미 맞는 칸은 건너뜁니다.
